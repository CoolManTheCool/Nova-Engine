{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Project",
            "type": "shell",
            "command": "${workspaceFolder}/build.sh"
        },
        {
            "label": "Clean Project",
            "type": "shell",
            "command": "${workspaceFolder}/clean.sh"
        }
    ]
}
